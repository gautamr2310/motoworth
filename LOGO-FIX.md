# MotoWorth Logo Fix

Updated the active MotoWorth `index.html` to use the supplied orange geometric MotoWorth mark.

The previous generated logo pseudo-element is explicitly disabled so it cannot overlay the new image. The logo asset also uses a cache-busting query string.

No calculator, valuation, catalogue, state-tax, PDF, API, or other application logic was changed.
